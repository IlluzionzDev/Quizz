import CenterSection from '@components/layout/CenterSection';
import type { NextPage } from 'next';

const CreateQuiz: NextPage = () => {
  return (
    <div>
      <CenterSection>Create Quiz</CenterSection>
    </div>
  );
};

export default CreateQuiz;
