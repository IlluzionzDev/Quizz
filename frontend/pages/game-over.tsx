import CenterSection from '@components/layout/CenterSection';
import type { NextPage } from 'next';

const GameOver: NextPage = () => {
    return (
        <div>
            <CenterSection>Game is over</CenterSection>
        </div>
    );
};

export default GameOver;
